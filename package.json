{
  "name": "hardhat-project",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "hh-deploy": "npx hardhat run --network localhost scripts/deploy.js",
    "hh-test": "npx hardhat test",
    "hh-compile": "npx hardhat compile --force",
    "hh-node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.2",
    "vite": "^3.0.9"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "ethers": "^5.7.0"
  }
}
